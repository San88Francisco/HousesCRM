name: CI/CD Pipeline

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  notify-on-pr:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Notify about pull request
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d parse_mode=Markdown \
            -d text="📥 Новий Pull Request: ${{ github.event.pull_request.title }}
            👤 Автор: ${{ github.actor }}
            🔗 https://github.com/${{ github.repository }}/pull/${{ github.event.pull_request.number }}"

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Run docker compose (tests/healthchecks)
        run: |
          docker compose -f docker-compose.yml up -d
          sleep 10
          docker compose ps

      - name: Notify failure (tests)
        if: failure()
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d parse_mode=Markdown \
            -d text="❌ Тести не пройшли у *${{ github.ref_name }}*
            🔗 https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

  deploy:
    if: github.ref == 'refs/heads/develop'
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Trigger deploy on Koyeb
        run: |
          curl -fsS -X POST https://app.koyeb.com/hook/deploy/${{ secrets.KOYEB_DEPLOY_HOOK }}

      - name: Install jq (for JSON parsing)
        run: |
          sudo apt-get update -y
          sudo apt-get install -y jq

      - name: Wait for Koyeb deployment to be healthy
        env:
          KOYEB_API_TOKEN: ${{ secrets.KOYEB_API_TOKEN }}
          KOYEB_SERVICE_ID: ${{ secrets.KOYEB_SERVICE_ID }}
          TIMEOUT: "600"   
          SLEEP: "5"       
        run: |
          set -euo pipefail

          echo "⏳ Очікуємо на успішний деплой сервісу ${KOYEB_SERVICE_ID} у Koyeb..."
          end=$((SECONDS + TIMEOUT))
          last_status="UNKNOWN"

          while [ $SECONDS -lt $end ]; do
            # беремо найсвіжіший деплой для сервісу
            RESP=$(curl -fsS \
              -H "Authorization: Bearer ${KOYEB_API_TOKEN}" \
              "https://app.koyeb.com/v1/deployments?service_id=${KOYEB_SERVICE_ID}&limit=1")

            STATUS=$(echo "$RESP" | jq -r '.deployments[0].status // .deployments[0].state // "UNKNOWN"' | tr '[:lower:]' '[:upper:]')
            DEPLOY_ID=$(echo "$RESP" | jq -r '.deployments[0].id // ""')

            if [ -n "$DEPLOY_ID" ]; then
              echo "• Deployment ${DEPLOY_ID} статус: ${STATUS}"
            else
              echo "• Не знайдено активних деплоїв, чекаємо..."
            fi

            case "$STATUS" in
              HEALTHY)
                echo "✅ Деплой HEALTHY."
                exit 0
                ;;
              ERROR|UNHEALTHY|STOPPED)
                echo "🚨 Деплой завершився зі статусом: ${STATUS}"
                exit 1
                ;;
              *)
                sleep "$SLEEP"
                ;;
            esac
          done

          echo "⏰ Таймаут очікування деплою в Koyeb."
          exit 1

      - name: Notify success (deploy)
        if: success()
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d parse_mode=Markdown \
            -d text="✅ *Деплой успішний* у *${{ github.ref_name }}*
            🔗 https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

      - name: Notify failure (deploy)
        if: failure()
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d parse_mode=Markdown \
            -d text="🚨 *Деплой провалено* у *${{ github.ref_name }}*
            🔗 https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
