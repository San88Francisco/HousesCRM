  deploy:
    if: github.ref == 'refs/heads/develop'
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install jq
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl ca-certificates

      # 1) Тригеримо redeploy через REST
      - name: Trigger redeploy (Koyeb REST)
        env:
          KOYEB_API_TOKEN: ${{ secrets.KOYEB_API_TOKEN }}
          KOYEB_SERVICE_ID: ${{ secrets.KOYEB_SERVICE_ID }}
        run: |
          set -euo pipefail
          curl -sS -X POST \
            -H "Authorization: Bearer $KOYEB_API_TOKEN" \
            -H "Content-Type: application/json" \
            "https://app.koyeb.com/v1/services/$KOYEB_SERVICE_ID/redeploy" \
            -o /tmp/redeploy.json
          echo "Redeploy triggered."

      # 2) Опитуємо останній деплой сервісу, чекаємо HEALTHY
      - name: Wait for deployment to be HEALTHY (Koyeb REST)
        env:
          KOYEB_API_TOKEN: ${{ secrets.KOYEB_API_TOKEN }}
          KOYEB_SERVICE_ID: ${{ secrets.KOYEB_SERVICE_ID }}
          TIMEOUT_SECONDS: "900"   # 15 хв
          SLEEP_SECONDS: "10"
        shell: bash
        run: |
          set -euo pipefail

          start_ts=$(date +%s)
          echo "⏳ Очікуємо HEALTHY…"

          while true; do
            # Беремо найсвіжіший деплой цього сервісу та читаємо його статус
            DEPLOY_JSON="$(curl -sS \
              -H "Authorization: Bearer $KOYEB_API_TOKEN" \
              "https://app.koyeb.com/v1/deployments?service_id=$KOYEB_SERVICE_ID&page_size=1")"

            STATUS="$(echo "$DEPLOY_JSON" | jq -r '.deployments[0].status')"
            echo "Поточний статус: ${STATUS}"

            if [[ "${STATUS}" == "HEALTHY" ]]; then
              echo "✅ Deployment is healthy."
              break
            fi

            if [[ "${STATUS}" == "UNHEALTHY" || "${STATUS}" == "FAILED" || "${STATUS}" == "ERROR" ]]; then
              echo "❌ Deployment status: ${STATUS}"
              exit 1
            fi

            now_ts=$(date +%s)
            if (( now_ts - start_ts > TIMEOUT_SECONDS )); then
              echo "⏱️ Таймаут очікування деплою."
              exit 1
            fi

            sleep "${SLEEP_SECONDS}"
          done

      - name: Notify success (deploy)
        if: success()
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="✅ *Деплой успішний* у *${{ github.ref_name }}* 🔗 https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

      - name: Notify failure (deploy)
        if: failure()
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="🚨 *Деплой провалено* у *${{ github.ref_name }}* 🔗 https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
